# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/quiz_app

# Optional individual PostgreSQL connection parameters
# (only needed if not using DATABASE_URL)
# PGUSER=your_username
# PGPASSWORD=your_password
# PGDATABASE=quiz_app
# PGHOST=localhost
# PGPORT=5432

# Session Configuration
SESSION_SECRET=quiz_app_secret_change_me_in_production

# Server Configuration
PORT=5000
HOST=0.0.0.0
NODE_ENV=development